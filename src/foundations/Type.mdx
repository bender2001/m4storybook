import { Meta } from "@storybook/blocks";
import { TypeRamp } from "./TypeRamp";

<Meta title="Foundations/Type" />

# Type scale

15 M3 type roles, mapped onto Tailwind's `text-*` utilities. Use
`text-display-l` through `text-label-s`. Each role carries the
M3 fontSize, lineHeight, letterSpacing, and weight.

<TypeRamp />
