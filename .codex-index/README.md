# Codebase Index

`codebase-index.json` contains file paths, languages, hashes, imports, exports, symbols, headings, short summaries, semantic terms, and embedding-ready text.

Use this file for fast AI orientation. A later vector store can embed each `embeddingText` value without re-walking the repository.
