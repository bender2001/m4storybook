import { Meta } from "@storybook/blocks";
import { ColorSwatchGrid } from "./ColorSwatchGrid";

<Meta title="Foundations/Color" />

# Color

M3 color roles are exposed as CSS variables (`--md-sys-color-<role>`)
and as Tailwind utility classes (`bg-primary`, `text-on-surface`, …).
The same role map is rendered in light and dark modes — toggle the
toolbar to compare.

<ColorSwatchGrid />
