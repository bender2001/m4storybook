import { Meta } from "@storybook/blocks";
import { ElevationLadder } from "./ElevationLadder";

<Meta title="Foundations/Elevation" />

# Elevation

Six elevation levels (0..5) emitted as Tailwind `shadow-elevation-*`
utilities. M3 Expressive promotes higher elevation only on press or
drag — keep resting elevation low for surfaces.

<ElevationLadder />
